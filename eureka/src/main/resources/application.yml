server:
  port: 8761
# 注册中心地址
spring:
  application:
    name: eureka
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    # 是否要在注册中心注册自己
    register-with-eureka: false
    # 是否要在注册中心拉取服务列表
    fetch-registry: false
